org.gradle.jvmargs = -Xmx4G
org.gradle.daemon = false
org.gradle.parallel = true
org.gradle.caching = true

# Mod Info
mod_version = 6.0.10
minecraft_version = 26.1

mod_id = create
mod_name = Create
mod_author = simibubi
mod_description = Technology that empowers the player.
mod_license = Read attached LICENSE.md
# thailand oil prices: https://oil-price.bangchak.co.th/BcpOilPrice1/en (bangkok retail no provincial tax included)
# america oil prices: https://fuelinsights.gasbuddy.com 
#if you reading this while complaining about fuel prices use them as a reference
# NeoForge
# https://neoforged.net/
#neoforged updated to 26.1 for porting create to 26.1
neo_version = 26.1.0.19-beta 

# removed parchment for 26.1 port as it is not compatible with 26.1 and has not been updated, and is not required for create to work (commented out instead of deleted in case it is updated in the future and can be re-enabled) #IThundxr DO NOT TOUCH UNTIL PARCHMENT 26.1 RELEASE IS AVAILABLE
parchment_version = none
parchment_minecraft_version = none
# Registrate
 registrate_version = none 
 # (cannot find available 26.1 release)

# Ponder
# https://maven.createmod.net/net/createmod/ponder/
# temporarily disabled ponder until ported
 ponder_version = none

# Flywheel & Vanillin
# https://maven.createmod.net/dev/engine-room/flywheel/
# create more like create 200,000 errors
flywheel_version = 1.0.6
flywheel_version_range = [1.0.0,2.0)
# https://maven.createmod.net/dev/engine-room/vanillin/
vanillin_version = 1.1.3-41

# Just Enough Items
# https://modrinth.com/mod/jei/versions
jei_version = none
  # (jei disabled for now until i port it to 26.1, as it is not required for create to work and has not been updated yet, but can be re-enabled in the future when it is updated)

# Curios
# https://modrinth.com/mod/curios/versions
# Curios updated to 15.0.0-beta.1+26.1 on 2026-03-26 for the 26.1 port, but is not required for create to work, so it can be optionally disabled by the user if they do not want to use it, and can be re-enabled in the future when it is updated for 26.1 if it is disabled
curios_version = 15.0.0-beta.1+26.1 

# Sodium
# https://modrinth.com/mod/sodium/versions
#sodium_version = 0.6.9 (sodium disabled for now until i port it to 26.1, as it is not required for create to work and has not been updated yet, but can be re-enabled in the future when it is updated)

# CC: Tweaked
# https://modrinth.com/mod/cc-tweaked/versions
#cc_tweaked_enable = true
#cc_tweaked_version = 1.116.1
#removed computer craft as it is not compatible with 26.1 and has not been updated, and is not required for create to work (commented out instead of deleted in case it is updated in the future and can be re-enabled)
# Farmer's Delight
# https://modrinth.com/mod/farmers-delight
#farmers_delight_enable = true
#farmers_delight_version = 1.21.1-1.2.9 (farmers delight disabled for now until i port it to 26.1, as it is not required for create to work and has not been updated yet, but can be re-enabled in the future when it is updated)
